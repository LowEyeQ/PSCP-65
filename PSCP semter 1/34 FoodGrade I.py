"""FoodGrade I"""
def main():
    """FoodGrade I"""
    chick = kukuk(float(input()))
    chick += kukuk(float(input()))
    chick += kukuk(float(input()))
    chick += kukuk(float(input()))
    chick += kukuk(float(input()))
    chick += kukuk(float(input()))
    chick += kukuk(float(input()))
    chick += kukuk(float(input()))
    chick += kukuk(float(input()))
    chick += kukuk(float(input()))
    chick += kukuk(float(input()))
    chick += kukuk(float(input()))
    chick += kukuk(float(input()))
    chick += kukuk(float(input()))
    chick += kukuk(float(input()))
    chick += kukuk(float(input()))
    chick += kukuk(float(input()))
    chick += kukuk(float(input()))
    chick += kukuk(float(input()))
    chick += kukuk(float(input()))
    chick += kukuk(float(input()))
    chick += kukuk(float(input()))
    chick += kukuk(float(input()))
    chick += kukuk(float(input()))
    print(chick)
def kukuk(chicken):
    """trueorflase"""
    if 50 <= chicken <= 70:
        amount = 1
        return amount
    else:
        amount = 0
        return amount
main()
